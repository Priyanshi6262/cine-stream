## Prompts

1. Write a JavaScript function using fetch to retrieve popular movies from the TMDB API and return the movie results.

2. the tmdb site is not working or loading in the browser how to resolve that how to generate the api key
 3. how to download the free vpn to access the tmdb site and generte the api key as well

 4.Implement a search bar in React that fetches movie results from the TMDB search API when the user types a movie name.

